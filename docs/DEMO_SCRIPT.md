# ProofLab demo script (target: 2:35)

## 0:00–0:15 — The problem

“A search result, persuasive AI prose, and a proof can look equally confident. ProofLab is a deterministic proof firewall that makes the evidence boundary visible.”

Show the one-line architecture: GPT-5.6 interprets; exact code decides.

## 0:15–0:48 — Refute the false family

Load **False family**. Point to the live GPT-5.6 label. Run it.

Show:

- GPT-5.6 extracted a parametric identity;
- exact substitution left residual `8*t^2`;
- `t = 1` is an exact counterexample;
- the final status is `DISPROVED`.

## 0:48–1:17 — Prove and replay the true identity

Load **True identity**. Run it, show residual `0`, and click **Replay certificate**.

Say: “The SHA-256 checksum detects edits; it is not a signature. The exact replay is what reproduces the evidence.”

## 1:17–1:43 — Global modular proof

Load **Modular impossibility**. Show the complete modulo-4 residue obstruction and why it proves integer non-existence globally.

## 1:43–2:06 — Memorable reveal

Click **Try to break this argument**.

Explain that GPT-5.6 proposes attacks, while deterministic tools execute them. Mention the prompt-injection eval that attempts to force a forged `PROVED`; the schema contains no such field.

## 2:06–2:23 — Honest refusal and education impact

Show that unsupported or side-conditioned mathematics returns `UNKNOWN` with reformulation guidance. Enter educator mode and point to the pre-verdict classification question and worksheet export.

## 2:23–2:35 — Build Week close

Show the baseline commit, 60-case GPT eval corpus, deterministic/fuzz/SymPy/browser gates, and Codex contribution in one rapid frame.

Close: “ProofLab does not make AI sound more certain. It makes mathematical certainty inspectable.”

## Recording checklist

- Public YouTube video, under three minutes, with clear audio.
- Fresh/incognito browser session.
- No hidden authenticated development state.
- Live route health says `GPT-5.6 connected`.
- Run the three golden cases immediately before recording.
- Keep copyrighted music and third-party trademarks out of the recording.
