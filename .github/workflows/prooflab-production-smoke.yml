name: ProofLab production smoke

on:
  deployment_status:
  schedule:
    - cron: "17 8 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Use live GPT-5.6 or reviewed offline obligations"
        type: choice
        options: [live, offline]
        default: live

permissions:
  contents: read

concurrency:
  group: prooflab-production-smoke
  cancel-in-progress: true

jobs:
  smoke:
    if: github.event_name != 'deployment_status' || (github.event.deployment_status.state == 'success' && github.event.deployment.environment == 'Production')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Exercise health, three golden cases, replay, and adversarial mode
        env:
          # Vercel preview/generated deployment URLs can require deployment protection.
          # The canonical public domain is the actual judge surface and needs no bypass secret.
          PROOFLAB_PRODUCTION_URL: https://www.diophantix.com
          PROOFLAB_SMOKE_MODE: ${{ inputs.mode || 'live' }}
        run: node --experimental-strip-types scripts/smoke-production.mjs
